// Este componente fue reemplazado por la página /contratos/[id]/page.tsx
// Se conserva como stub para evitar errores de importación desde contratos/page.tsx
// que aún lo importa pero ya no lo renderiza (el botón "Ver" ahora es un Link).

export default function ContratoDetalle(_props: { contrato: unknown; onClose: () => void }) {
  return null;
}
